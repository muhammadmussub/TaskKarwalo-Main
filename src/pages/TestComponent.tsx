// Test file to verify syntax
import React from 'react';

const TestComponent = () => {
  return (
    <div>
      {true && (
        <div>
          Test
        </div>
      )}
    </div>
  );
};

export default TestComponent;